<h1> <%= @job.title %></h1>

<p>
  <%= simple_format(@job.description) %>
</p>
<div class="text-center">
  <%= link_to("投递简历", new_job_resmue_path(@job), :style => "font-size: 30px; text-decoration:underline;") %>
</div>
